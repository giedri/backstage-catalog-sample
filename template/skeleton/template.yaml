AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: ${{ values.description }}

Parameters:
  AuthIssuerUrl:
    Type: String
    Description: JWT token issuer URL (e.g. https://cognito-idp.us-east-1.amazonaws.com/us-east-1_xxx)
  AuthAudience:
    Type: CommaDelimitedList
    Description: Accepted JWT audience values (e.g. app client IDs)

Globals:
  Function:
    Runtime: python3.12
    MemorySize: 256
    Timeout: 30
    Environment:
      Variables:
        LOG_LEVEL: INFO
        STACK_NAME: !Ref AWS::StackName
        TABLE_NAME: !Ref ItemTable
    Architectures:
      - arm64

Resources:
  # ---------- API Gateway ----------
  ServerlessHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      Auth:
        DefaultAuthorizer: JwtAuthorizer
        Authorizers:
          JwtAuthorizer:
            AuthorizationScopes: []
            IdentitySource: $request.header.Authorization
            JwtConfiguration:
              issuer: !Ref AuthIssuerUrl
              audience: !Ref AuthAudience

  # ---------- DynamoDB ----------
  ItemTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${AWS::StackName}-items"
      BillingMode: PAY_PER_REQUEST
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: sk
          AttributeType: S
        - AttributeName: gsi1pk
          AttributeType: S
        - AttributeName: gsi1sk
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
        - AttributeName: sk
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: gsi1
          KeySchema:
            - AttributeName: gsi1pk
              KeyType: HASH
            - AttributeName: gsi1sk
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
      TimeToLiveSpecification:
        AttributeName: ttl
        Enabled: true
      Tags:
        - Key: Service
          Value: !Ref AWS::StackName

  # ---------- Lambda Functions ----------
  CreateItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src.handlers.create_item.lambda_handler
      CodeUri: .
      Description: Creates a new item
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ItemTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /v1/items
            Method: POST
            ApiId: !Ref ServerlessHttpApi

  GetItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src.handlers.get_item.lambda_handler
      CodeUri: .
      Description: Retrieves an item by ID
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ItemTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /v1/items/{itemId}
            Method: GET
            ApiId: !Ref ServerlessHttpApi

  ListItemsFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src.handlers.list_items.lambda_handler
      CodeUri: .
      Description: Lists items with pagination
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ItemTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /v1/items
            Method: GET
            ApiId: !Ref ServerlessHttpApi

  DeleteItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src.handlers.delete_item.lambda_handler
      CodeUri: .
      Description: Deletes an item by ID
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ItemTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /v1/items/{itemId}
            Method: DELETE
            ApiId: !Ref ServerlessHttpApi

  HealthFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src.handlers.health.lambda_handler
      CodeUri: .
      Description: Health check endpoint
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /health
            Method: GET
            ApiId: !Ref ServerlessHttpApi
            Auth:
              Authorizer: NONE

Outputs:
  ApiUrl:
    Description: API Gateway endpoint URL
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.amazonaws.com"
  ItemTableName:
    Description: DynamoDB table name
    Value: !Ref ItemTable
  ItemTableArn:
    Description: DynamoDB table ARN
    Value: !GetAtt ItemTable.Arn
